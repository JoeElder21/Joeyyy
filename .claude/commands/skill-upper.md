---
description: Validate or run governed Agent Skill evaluations through skill-up
argument-hint: "[validate|list|run] [approved skill eval path]"
allowed-tools: Bash(scripts/skill_up.sh:*), Read, Grep, Glob
---

Read the repository-root `AGENTS.md` and
`.agents/skills/skill-upper/SKILL.md`. Classify the target brain first, keep
APEX and JEOS evaluation evidence separate, and route the final result through
Agent 007. Default to `scripts/skill_up.sh validate`; run only an approved,
public-safe or private-authorized suite. Never configure credentials, enable
telemetry, publish reports, promote a skill, or apply an evolution repair
without the mission's mutation authority and independent review.
