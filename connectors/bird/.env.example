# Copy to .env on the workstation. Never commit the real key.
# Bird keys look like bk_xxxxxxxxx — that shape is a placeholder, not a secret.
BIRD_API_KEY=
BIRD_FROM=
BIRD_HELLO_TO=
BIRD_SEND_HELLO=0
BIRD_SEND_OTP=0
BIRD_OTP_TO=
