# Optional production runtime for the AutoGen orchestration adapter.
# The adapter remains importable without this package so repository-only
# validation never creates an unverified connector or model-access claim.
# Repository tooling and PacketGuard require Python 3.11–3.12.
# Use Microsoft's official distribution for the legacy AutoGen 0.2 API.
autogen-agentchat>=0.2.35,<0.3; python_version >= "3.11" and python_version < "3.13"
# REQUIRED for privacy validation, not optional. scripts/privacy_guard.py
# reconstructs YAML and JSON values through this parser, and without it every
# file declaring those formats is reported as an incomplete scan rather than
# passed -- deliberately, because the alternative was a local gate weaker than
# CI: fallback regexes cannot decode an escaped credential key, so a bare run
# on a developer machine approved files CI rejects. Install it before running
# the guard. The rest of the suite still runs stdlib-only; the parser-dependent
# tests skip.
PyYAML>=6.0
