# Gap 3 — policy enforcement at runtime + connector baseline.
# Canonical sources: pydantic/pydantic, python-jsonschema/jsonschema,
# samuelcolvin/rtoml, modelcontextprotocol/python-sdk, anthropics/anthropic-sdk-python.
pydantic
jsonschema
rtoml
# Held below 2.0: `scripts/governance_mcp_server.py` builds its server from
# `mcp.server.fastmcp`, and 2.0.0 removes that module outright --
# `ImportError: No module named 'mcp.server.fastmcp'`. The manifest carried a
# bare `mcp`, so re-resolving the lock crossed the major boundary on its own and
# the mounts job failed with a bare `NameError: name 'build_server' is not
# defined`. A range is the honest statement of what this code supports; lifting
# it is a port of the server, not a version bump.
#
# Dependabot PR #90 rewrote this to `mcp<3` and pinned `mcp==2.0.0`. That is
# the same break. Keep the 1.x line until the server is ported.
mcp<3
# Held below 1.0: Dependabot #90 grouped anthropic 0.120.2 -> 1.0.0 with the
# mcp major. That SDK major is a separate port, not this hygiene bump.
anthropic<2
# Patch hygiene from #90; pyjwt pulls this in. Not a major.
cryptography>=50.0.1
