"""Verified-runtime adapters for Agent 007.

Adapters in this package are deliberately connector-free until a caller passes
verified runtime dependencies.  They must not be treated as evidence that an
external service, memory store, or agent runtime is available.
"""
